const fs = require('fs');
const path = require('path');

const frontHandler = (request, response) => {
  // Marwa And Yasmin
}

const searchHandler = (request, response) => {
  // Sum3a and Sultan
}

module.exports = {
  frontHandler,
  searchHandler
}
